---
import OpenSourcePage from "../../components/OpenSourcePage.astro";
export function getStaticPaths() {
  return ["en", "zh-cn", "zh-tw"].map((lang) => ({ params: { lang } }));
}
---

<OpenSourcePage locale={Astro.params.lang} />
